# Adversarial Review Log — Modular Rebuild

Running, append-only checkpoint log for the adversarial review of the `opensearch-mcp-server-py` modular rebuild (`main..HEAD`). Each entry is a zoomed-out, human-facing verdict on rebuild quality at a point in time. Newest entries are appended at the bottom.

---

## [checkpoint] — On track and faithful, but two ordering/parser defects and ~1100 LOC of inert scaffold need attention before wire-in  (P0-P3)

**Bird-eye OLD-vs-NEW.** The rebuild is on track and the actually-wired work is a genuine net simplification: deleting the runtime OpenAPI tool generator (boot-time unpinned GitHub fetch, no timeout, `print()`-into-JSON-RPC hazard) and replacing it with four golden-snapshot-locked static tools is the right trade, and extracting `compat.py` as a leaf cleanly breaks the real `tools→generic_api_tool→tool_filter→tools` import cycle without lazy-import dodges. Fidelity discipline is unusually strong — the request handler is a byte-for-byte port (NDJSON spacing, GET-with-body, plain `json.dumps`), the version-gate `'1.0'` semantics are preserved, and all 684 tests pass with zero outbound network at import. The main drag on the checkpoint is that it also lands ~1100 LOC of deliberately-UNWIRED machinery (settings/auth_strategy/version_cache/context/registry/modules) plus ~1700 lines of overlapping (and partly superseded) planning docs alongside the still-live originals, so none of those abstractions has earned its keep yet and several carry latent defects. Net: faithful and simpler where it touches production, but accumulating more "present but inert" surface than a "easy to contribute" mid-flight codebase should carry.

### Findings

| Severity | Lens | Finding | Recommendation |
|---|---|---|---|
| MAJOR | Architecture / Fidelity / Code-Quality / Simplicity (all four lenses) | **Generated-tool order has two conflicting definitions and the observable `tools/list` order is unverified.** `register.py:62-109` builds dict order Msearch/Explain/Count/ClusterHealth and `test_generated_tools_golden.py:51-53` pins exactly that as "the generator's order," but the only consumer `_register_generated_tools()` (`tools.py:1303`) re-inserts via a *different* hard-coded tuple `('ClusterHealthTool','CountTool','MsearchTool','ExplainTool')` — verified live as registry positions 44-47. The golden fixture is stored alphabetically, so it is not an order oracle. The true pre-deletion order (cluster.yaml→ClusterHealth, then `_core.yaml` op iteration) rests on a CHANGELOG sentence, not a verified snapshot. Two sources of truth, one tested-but-dead, neither pinning the wire contract. | Pick one owner: make `_register_generated_tools` iterate `build_generated_tools(...)` in returned order and delete the tuple in `tools.py:1303`, AND/OR add a test asserting `list(TOOL_REGISTRY.keys())[-4:]` equals the order the old `serve()` path produced. Capture the real pre-deletion order from git history of the live spec; do not trust the CHANGELOG prose. |
| MAJOR | Code-Quality / Fidelity | **`settings.py` truthy-union parser silently widens the truthy set for security flags; docstring claim is false.** `_TRUTHY_VALUES={'true','1','yes'}` is applied to `opensearch_no_auth`, `aws_opensearch_serverless`, `opensearch_header_auth`, `memory_tools_enabled`, `opensearch_settings_allow_write` — all parsed in live code as `value.lower()=='true'`. Empirically `Settings(OPENSEARCH_NO_AUTH='YES').opensearch_no_auth == True`, so a typo'd `1`/`yes` now disables auth. The docstring's "no input that is truthy today becomes falsy (and vice versa)" is provably wrong, and the "observed in the current codebase" justification is unsupported (no governed flag ever accepted `1`/`yes`). Staged drift the next phase inherits silently. | Drop `1`/`yes` from `_TRUTHY_VALUES` so these 5 flags are exactly `lower()=='true'` (truly faithful), OR keep the union only with an explicit documented decision and fix the false docstring claims. Add a test pinning `OPENSEARCH_NO_AUTH='1'`/`'yes'` to whichever semantic is chosen. |
| MAJOR | Code-Quality / Architecture | **`version_cache` holds one global `asyncio.Lock` across the awaited fetch — cross-key head-of-line blocking, effectively untested.** `version_cache.py:174-184` does `async with _LOCK:` then `await fetch()` while holding the single module-global lock, so a slow/hung `GET /` for cluster A blocks version resolution for B/C/D in multi mode (and the underlying `get_opensearch_version` has no timeout). The docstring sells "N calls collapse to one fetch" (true only per-key) and never discloses cross-key serialization. The only concurrency test uses a synchronous counter (no await/sleep), so it would pass even with full serialization — a tautology. | Use a per-key lock (dict of `asyncio.Lock` under a short meta-lock, or double-checked pattern) so independent targets fetch concurrently; OR document the cross-key blocking in the docstring and add a regression test with an awaitable slow fetch proving key B is not blocked by pending key A. Wire a timeout into `get_opensearch_version` before this goes live. |
| MAJOR | Architecture / Simplicity | **~1100 LOC of unwired abstraction shipped alongside live duplicates — none earns its keep yet.** `settings.py`(290), `auth_strategy.py`(280), `version_cache.py`(193), `registry.py`(160), `modules.py`(84), `context.py`(63) have zero production import references (grep-confirmed) while the code they replace is fully live (inline 6-level auth ladder, `global_state` with 23 call sites, uncached per-call version fetch, 344-line dict literal). Risk is silent drift: e.g. `auth_strategy.py` hand-copies precedence from `client.py` and will diverge if `client.py` changes first. Violates the project's own minimal-diff constraint #2 and DECISION_LOG D6's "must replace, not add." | Sequencing call: either (a) wire each module in the same checkpoint it is introduced, or (b) add guardrail tests pinning each unwired module against its live counterpart (e.g. parametrized inputs through `resolve_auth_strategy` vs the inline ladder) so divergence fails loud before wire-in. |
| MAJOR | Simplicity | **Unwired default-arg fallback in `register.py` is untested defensive code with a latent circular import.** `register.py:57-58`: `if version_check is None: from tools.tools import check_tool_compatibility` — never used in production (caller always injects) and never tested (golden test injects a noop). It imports from `tools.tools`, the exact module that imports `build_generated_tools` at import time, so it is a dormant import-cycle footgun for zero benefit. | Make `version_check` a required parameter (drop the `Optional`/`None` default and the lazy import). Both the sole caller and the test already pass it explicitly. Removes the cycle risk and the untested branch in one edit. |
| MINOR | Architecture / Fidelity / Code-Quality | **Import-time mutation of `TOOL_REGISTRY` is a hidden side-effect and a boot-time→import-time timing change.** `tools.py:1307` calls `_register_generated_tools()` at module bottom, so importing `tools.tools` now mutates the global; the old generator ran at `serve()`/`create_mcp_server()` time. Verified benign (consumers deepcopy via `apply_custom_tool_config`, registration is idempotent), but a failure in `build_generated_tools` would now surface as an ImportError on any consumer, broadening blast radius. DECISION_LOG D7 does not call out the timing shift. | Document the import-time contract at the `TOOL_REGISTRY` definition site / DECISION_LOG D7 (idempotent, read-only post-import). Long-term, let the eventual `compose_registry`/`build_registry(ctx)` boot pipeline own registration so the import-time side effect disappears. |
| MINOR | Architecture | **`compose_registry` pinning test is tautological — the manifest does not yet own the order.** `test_modules.py:42` sources the `core` group *from* `tools_mod.TOOL_REGISTRY`, then asserts `composed.keys() == legacy.keys()`; since it re-splats the same objects in the same order it only proves splatting preserves order, not that the manifest matches the legacy literal. `GROUP_ORDER` is currently decorative. | When per-domain `register()` modules land, source `core` from the new domain modules (not `TOOL_REGISTRY`) so the manifest becomes an independent oracle. Mark the test as a placeholder until then so it is not mistaken for real coverage. |
| MINOR | Code-Quality / Architecture | **`auth_strategy.py` has a dead defensive branch and a silently-divergent fallback exception class.** `resolve_auth_strategy:246` `if _is_blank(aws_region): raise ...` is unreachable (guarded by `has_region = bool(aws_region)` already True). Separately, lines 33-49 define a fallback local `AuthenticationError` if the import from `client.py` fails (`pragma: no cover`); if it ever triggers, `except AuthenticationError` in `client.py` would not catch resolver errors due to class-identity mismatch. | Remove the dead branch (or use `has_region = not _is_blank(...)` to handle whitespace-only regions consistently). Replace the fallback exception with a hard ImportError at module load — a partially-initialized auth module is fail-fast, not degrade-gracefully. |
| MINOR | Simplicity | **`modules.py` is a 4-element manifest wrapping a dict iteration — indirection heavier than its value.** `GROUP_ORDER` + `compose_registry(*, skills, agentic_memory, memory, core)` rebuild a dict then iterate the tuple to call `registry.update`, restating what the fixed-order kwargs already fix. Four explicit `registry.update(...)` calls would be equally explicit and shorter. Unwired. | When wiring, consider collapsing to four explicit `update` calls in order, keeping `GROUP_ORDER` only if the group set is expected to grow dynamically. Not blocking. |
| MINOR | Simplicity | **The 4 static tools were split into a 6-file nested package where one file would read more simply.** The 317-line `tool_generator.py` became `domains/generated/` = schema(124)+params(48)+handlers(230)+register(111)+2×`__init__` ≈ 513 LOC across 6 files for 4 tightly-coupled tools. | Acceptable only if every domain follows the same shape for consistency. Otherwise consider merging schema+params+register (static data) into one file, keeping handlers separate. |

### What is genuinely good
- **`compat.py` is dependency inversion done well.** Moving `check_tool_compatibility` to a leaf taking injected `registry` + `version_fetcher` genuinely breaks the `tools→generic_api_tool→tool_filter→tools` cycle without lazy-import dodges; the 1-line delegator keeps ~40 call sites untouched, and the bare-`Exception` raise + exact message text are reproduced byte-for-byte (pinned by `test_compat.py`).
- **The handler port is verbatim, verified line-by-line against `main:src/tools/tool_generator.py`.** `process_body` (NDJSON spacing), `select_endpoint` (method-ignoring GET-with-body), base-arg extraction (missing→`''`), path substitution, and plain `json.dumps` (not the compact `format_json`) are character-for-character identical — a less careful port would have silently changed every response's separators/ensure_ascii.
- **The golden-snapshot lock is a strong runtime→static migration oracle.** Captures generator output before deletion and asserts dict-equality on `input_schema` plus byte-exact `display_name`/`description`/`http_methods`/`min_version('1.0')`/`max_version('99.99.99')`, including correctly NOT normalizing `'1.0'`→`'1.0.0'` and correctly OMITTING `required` when empty (vs emitting `required: []`).
- **Deliberate fidelity over "helpful" fixes.** The `/None/_explain/{id}` garbage-path quirk and loose `Optional[str]=None` arg models are preserved, not silently tightened — exactly right for a fidelity checkpoint.
- **Clean value objects and drop-ins.** `ServerContext` (frozen+slots dataclass mirroring each `global_state` field, with the `get_mode()`-defaults-to-`single` footgun diagnosed in its docstring); `ToolRegistry` (full dict read API, fail-loud `DuplicateToolError`, `as_dict()` shallow copy so deepcopying consumers are unaffected); `version_cache._normalize_url` (scheme/host lowercasing, default-port insertion, IPv6 bracketing, userinfo/trailing-slash stripping, injected monotonic clock for deterministic TTL tests).
- **`settings.py` per-variable fidelity is right in spirit.** It preserves inverted `OPENSEARCH_SSL_VERIFY` ('True unless "false"'), tri-state `OPENSEARCH_DYNAMIC_CONNECTION` left raw, and no-try/except `OPENSEARCH_TIMEOUT` that still raises — the correct instinct for a no-drift rebuild (modulo the truthy-union MAJOR above).
- **The `#271` `/mcp` no-307 fix is minimal and provably protocol-neutral.** `Route('/mcp')` + `Mount('/mcp')` with a tiny `_ASGIApp` wrapper; confirmed `StreamableHTTPSessionManager.handle_request` never inspects `scope['path']`, and `test_mcp_endpoint_no_redirect` asserts 200 with no `location` header.
- **Staying on low-level `Server`** (vs a FastMCP port) is correctly justified by per-mode schema mutation + SDK v2 churn, and the docstrings are unusually honest about each module's inert status and residual risk.

### Decisions to revisit (QUESTIONs for the human)
- **`required`-array ordering normalized to deterministic.** The static schemas hard-code `required` order (e.g. `['body','id','index']`); the old generator emitted set-iteration (non-deterministic) order. Benign and semantically equivalent (JSON-Schema treats `required` as a set), but clients now receive stable bytes where they previously did not. Confirm the captured order matches what real clients received and log it as a deliberate, benign normalization (DECISION_LOG D2/D7).
- **Frozen `min_version='1.0'` / `max_version='99.99.99'`.** The old code read `x-version-added`/`x-version-deprecated` from the live spec every boot; the static tools freeze these forever. Intended consequence of de-pinning (P0-1) and `is_tool_compatible` parses `'1.0'`≡`'1.0.0'`, so no gate math drift — but confirm this freeze of an upstream-tracking field is a conscious sign-off.
- **`auth_strategy` fail-secure raise is a real behavior change.** It RAISES on partial AWS creds (access/secret without the full triple) where `client.py` previously fell through to IAM/basic/ambient. This is the documented O4 decision and the right call, but the eventual wire-in PR must flag it in CHANGELOG/release notes as a breaking change for partial-credential configs.
- **`version_cache` proportionality.** 193 lines (negative-TTL floor, clock injection, IPv6-aware normalizer) for a per-call-version-fetch churn bug — and DESIGN_DECISIONS itself notes the cache does NOT fix the real correctness hole (version=None still enables 3.3.0+ tools). Confirm the churn is a measured problem before wiring this much machinery; a plain TTL dict keyed on URL may suffice for v1.
- **Sequencing of scaffold vs wire-in.** Is shipping P2 modules unwired-and-untested-against-live (then migrating in later phases) the intended cadence, or should each phase wire what it introduces? This governs how much latent drift risk the tree carries between checkpoints.

### VERDICT
**PROCEED-WITH-FIXES** — fix the dual-source tool-ordering (add a `TOOL_REGISTRY` tail-order test) and the `settings.py` truthy-union/docstring before any wire-in; the wired work itself is faithful and sound.

---

## [checkpoint #2] — Wired work got better (version_cache, connection redesign, auth fixes), but a real crash-masking regression landed and the modular scaffold is still inert  (P0-P3, post review #1)

**Bird-eye OLD-vs-NEW.** The rebuild remains on track and the half that is actually wired improved measurably since review #1: review #1's global-lock concern was addressed (`version_cache` now uses a per-key `asyncio.Lock` with a lock-free fast path and is wired into `get_opensearch_version`), the `connection.py` response-size path was redesigned into a genuine net simplification (`None`-limit short-circuits fully to `super()`, the dangerous request-re-issuing `_fallback_perform_request` was deleted, decode switched to `('utf-8','surrogatepass')` to match opensearch-py), the auth fixes are surgical and fail-secure, and `auth_strategy.py` was correctly BUILT-THEN-DELETED (D11) after an adversarial workflow disproved its "duplicated ladder" premise — exactly the minimal-diff instinct the mandate asks for. But the connection.py rewrite introduced a confirmed crash-masking regression, the four headline modular abstractions (`registry.py`, `modules.py`, `context.py`, `settings.py` ≈ 600 LOC) remain entirely unwired with zero production importers while the monolith they target is fully live, and the promised per-domain `core.py`/`cat.py`/`bootstrap.py`/`serve.py` split still does not exist (only `domains/generated/` landed). Net: the wired surface is faithful and now cleaner, but this checkpoint ships one BLOCKER and accrues more "present but inert" structure under a simplicity mandate than it retires.

### Findings

| Severity | Lens | Finding | Recommendation |
|---|---|---|---|
| BLOCKER | Minimal-Diff / Code-Quality | **`connection.py` catch-all handler can raise `UnboundLocalError` and swallow the real transport error.** Verified at `connection.py:306-318`: the `except Exception as e:` handler references `start` (309), `url_path` and `orig_body` (310), all assigned INSIDE the `try` (`orig_body`/`url_path` at 170-171, `start` at 203) AFTER `_create_aiohttp_session()` (166), `urlencode` (173), gzip (192), and the auth callable (200). If any of those raises — the most realistic being a session/SSL setup failure, i.e. *exactly* the transport failure this block exists to translate into `ConnectionError`/`SSLError` — line 309 raises `UnboundLocalError` and the original exception is lost. The deleted `_fallback_perform_request` set its timer at the very top before its `try`, so this is a regression introduced by the rewrite. The early-abort test mocks `_create_aiohttp_session` as a never-throwing AsyncMock, so the path is untested and CI is green. | Hoist `start = self.loop.time()`, `orig_body = body`, and `url_path = self.url_prefix + url` to just before the `try:` at line 163 (or guard the handler with local fallbacks). Add a test that makes `_create_aiohttp_session` (or the auth callable) raise and asserts a `ConnectionError`/`SSLError`/`ConnectionTimeout` propagates — not `UnboundLocalError`. |
| MAJOR | Fidelity / Code-Quality | **`params.py` validation silently diverges from the generator and its own docstring contradicts the code.** Confirmed at `params.py:23-49`: fields are `Optional[str]=None` / `Optional[Any]=None`, which ACCEPT explicit `index=None`. The deleted generator used `create_model(index=(str, None), …)` — Pydantic v2 types that as `str` with default `None`, which REJECTS explicit `None` with a ValidationError. So all 4 ex-generated tools (Explain/Count/Msearch/ClusterHealth) now validate explicit-null path/index args where they previously errored, reachable via `tool_executor`. The docstring (lines 8-11) explicitly and falsely claims "we mirror that … so the same inputs validate the same way." Not caught by the golden test (which only diffs `input_schema`, never the args model), and not in the O-list. | Either (a) restore exact fidelity by building the models the generator's way (`str`-typed, `None` default, rejecting explicit null), OR (b) keep the looser model but fix the false docstring, add a DECISION_LOG observable entry (e.g. O5: "explicit-null path/index args on the 4 ex-generated tools now validate instead of erroring") flagged for maintainer ratification, and add a unit test pinning the chosen behavior. Do not leave a docstring asserting fidelity the code contradicts. |
| MAJOR | Code-Quality / Architecture | **`version_cache` key ignores the header-auth-resolved URL → cross-cluster version bleed in single mode.** `make_cache_key` resolves the single-mode key from `args.opensearch_url` / `OPENSEARCH_URL` only, but when `OPENSEARCH_HEADER_AUTH=true` the actual target comes from the per-request `opensearch-url` header inside `_initialize_client_single_mode` — which the key never sees. Two calls hitting physically different clusters via different headers share one cache entry, so a `Version` fetched against cluster A is returned for cluster B for up to the TTL (default 600s), wrongly allowing/denying the version gate. The pre-cache code re-fetched `GET /` against the real client every call, so the cache introduces this regression; the autouse `clear_cache()` fixture also masks any cross-call bleed in the suite. | Factor URL resolution (env + header override) into a shared helper both `client.py` and `make_cache_key` call so the header URL partitions the key, OR bypass the cache when header-auth/dynamic per-call URL targeting is in effect. At minimum add a test driving two distinct `opensearch-url` headers through `get_opensearch_version` asserting distinct fetches, and document the limitation if deferred. |
| MAJOR | Architecture / Simplicity / Minimal-Diff | **~600 LOC of headline modular abstraction is still completely unwired; the live duplicate-overwrite behavior is unchanged.** `registry.py`(160), `modules.py`(84), `context.py`(63), `settings.py`(293) have zero non-test importers (grep-confirmed). The live catalog is still the legacy dict in `tools.py`, and the 4 generated tools are appended via direct `TOOL_REGISTRY[name]=spec` in `_register_generated_tools()` — the exact silent last-writer-wins path `ToolRegistry.add`'s fail-loud duplicate detection claims to eliminate, so that protection is NOT active. Mode is still read from `global_state.get_mode()` at ~5-9 live sites; config still reads env directly. Since review #1, `auth_strategy` was deleted (good) and `version_cache` wired (good), but the registry/context/settings trio remains the bulk of the claimed "modular shape" and earns nothing the live code uses. The promised per-domain `core.py`/`cat.py`/`search_relevance.py` register modules and `bootstrap.py`/`serve.py` do NOT exist yet. | Per the project's own D6/D10 minimal-diff mandate: land each module in the SAME unit that wires it AND deletes the code it replaces (so each is a net-neutral swap a reviewer can evaluate), or keep them on a not-yet-merged branch until then. If they must ship early, add a `# UNWIRED — consumed in Phase P4` banner at the top of each so a contributor isn't misled into thinking they're load-bearing. Resist adding more registry surface area before a consumer exists. |
| MINOR | Architecture / Code-Quality / Minimal-Diff | **`context.py` (`ServerContext`) is dead-on-arrival and creates a second, divergent representation of `mode`.** The frozen dataclass claims to "replace the scattered process-global mutable state," but `global_state` remains the sole authority (read in `tool_filter`, `tool_params`, `client`, `helper`, `server_instructions`; set in both servers). It is referenced only by its own test, re-encodes the same `get_mode()`-defaults-to-`single` footgun its docstring criticizes, and is the weakest-earning of the four unwired modules. | Defer `context.py` until the bootstrap/serve phase that constructs it; reintroduce it in the commit that converts the first reader (e.g. `helper.get_opensearch_version`, which already takes mode) from `get_mode()` to `ctx.mode`, so it has a single source of truth from day one. |
| MINOR | Architecture / Code-Quality / Fidelity | **"byte-for-byte" docstring claims overstate what the golden test verifies, and `required` order is locked to a normalized (alphabetical) snapshot, not the generator's set-derived order.** `schema.py`/`register.py` claim byte-equality, but the golden test compares `input_schema` with order-independent dict `==` against a snapshot the test itself stored with sorted keys. The generator emitted `required` from a `set` (nondeterministic order); the new code hardcodes alphabetical (`['body','id','index']` / `['body']`). Wire-different but semantically inert (JSON-Schema `required` is a set). Carried over from review #1's QUESTIONs but the docstrings still imply verbatim reproduction. | Reword the docstrings to "dict-equal to the captured snapshot" and add a one-line note that `required` order is intentionally normalized and semantically a set. Optionally compare `required` as `set(...)` in the test to make the oracle honest. No behavior change needed. |
| MINOR | Code-Quality | **Unbounded growth of `_CACHE`/`_LOCKS` under single-mode dynamic connections.** Both module dicts are keyed per target with no eviction/maxsize/sweep; TTL only governs the stored expiry timestamp, never deletion, and `_LOCKS` never shrinks. A long-lived server fronting many dynamic per-call URL targets leaks a permanent entry per distinct URL. `clear_cache()` is a test-only hook. Pre-cache code held zero such state. | Bound the maps (LRU/maxsize, or delete expired keys on the slow-path miss) and evict the matching `_LOCKS` entry. |
| MINOR | Code-Quality | **`settings.parse_optional_int` docstring claims it "raises `ValueError` … matches today" but pydantic surfaces `ValidationError`.** Verified: `Settings(opensearch_timeout='abc')` raises `pydantic_core.ValidationError`, not the bare `ValueError` today's `int(x)` produces. Latent while `settings` is unwired, but a wire-in relying on the documented exception type would break. | Correct the docstring to state pydantic wraps the `ValueError` in `ValidationError`, and flag for the config wire-in phase that call sites catching `ValueError` on timeout parsing must be updated. |
| MINOR | Simplicity | **Repo root is accumulating ~8 rebuild-process `.md` artifacts that obscure the contributor-facing docs.** REBUILD_MASTER_PLAN, DECISION_LOG, AUDIT_FINDINGS, DESIGN_DECISIONS, ERROR_LOGGING_EVALUATION, FASTMCP_REBUILD_{DESIGN,PLAN}, MCP_V2_EXPLAINER, ADVERSARIAL_REVIEW_LOG sit alongside README/CONTRIBUTING/USER_GUIDE in the root; a new contributor can't tell onboarding docs from internal scratch. (`node_modules/` also appears untracked in the working tree.) | Move rebuild-process docs under `docs/rebuild/` (or `.github/`) before cutover and confirm `node_modules` is gitignored. Keep root `.md` to the standard OSS set. |

### What is genuinely good
- **Review #1's MAJOR lock finding is genuinely fixed.** `version_cache` now uses a per-key `asyncio.Lock` with double-checked locking and a lock-free fast path; the meta-lock is provably never held across the awaited fetch, and the slow-fetch-A-must-not-block-B test parks on a real `asyncio.Event` (not a synchronous counter), so it would actually fail under a single global lock. The negative-TTL floor (`min(ttl, 5s)`) with a test proving a `None` is re-fetched after the floor while a value is held for the full TTL is a thoughtful anti-stampede detail.
- **`connection.py` is materially better architecture (modulo the BLOCKER).** The `max_response_size is None` short-circuit to `super()` cleanly inherits parent auth/TLS/gzip/exception-translation with no second pass; deleting `_fallback_perform_request` removes a genuine non-idempotent-request hazard (a single 404 was 2 HTTP requests); the `surrogatepass` decode precisely matches opensearch-py; and the selective `reraise_exceptions` / `TransportError`-reraise / generic-translate faithfully reproduces opensearch-py's mapping. Net `-16` lines.
- **The auth fixes are surgical and correct.** Partial-AWS-key fail-secure guard correctly excludes region-alone; the bearer header is MERGED (preserving the custom User-Agent) rather than clobbering `client_kwargs['headers']`; IAM ARN moved INFO→DEBUG; and `_scrub_url_userinfo()` is applied at every URL log sink (CWE-532).
- **D11: build-then-delete `auth_strategy.py` is YAGNI discipline done right.** An adversarial workflow disproved the "duplicated 6-level ladder" premise (the ladder exists once), so a `+53/-9` in-place `client.py` fix replaced a ~320-line wired abstraction for the same O4 correctness — ~30x less diff, reasoning recorded. Exactly the "prefer in-place over new abstraction" the user asked for.
- **The generator deletion is verified-faithful on the wire contract.** The 4 static tools match the golden snapshot on order (ClusterHealth, Count, Msearch, Explain), descriptions, `http_methods` (`', '.join(sorted(set(...)))`), raw versions (`'1.0'`/`'99.99.99'`, not normalized), body descriptions, and the no-`type`-key body quirk; the handler (`process_body` NDJSON, method-ignoring `select_endpoint` GET-with-body, path substitution, plain `json.dumps`) is a verbatim port with behavior tests. Zero boot-time network; registration is now strictly more robust than the old silently-tool-dropping boot fetch.
- **`compat.py` injection design will pay off.** Injecting `registry` + `version_fetcher` (rather than importing them) is a clean leaf seam that already lets `domains/generated/register.py` call the gate without importing `tools.tools`, and the error text is reproduced byte-for-byte.
- **The DECISION_LOG (D0-D12) and standing adversarial loop give the rebuild real auditability.** Observable changes (O2/O3/O4 + log hygiene) are enumerated and flagged for ratification rather than slipped in — the correct posture for a no-drift mandate, and the DECISION_LOG pre-registers the unwired-scaffold risk (D6, D10 #4) honestly.

### Decisions to revisit (QUESTIONs for the human)
- **`version_cache` 600s default staleness window on the version gate.** A cluster upgraded/downgraded mid-session is gated against a stale version for up to 10 minutes (TTL configurable, `0` disables). Documented in DESIGN_DECISIONS but not in the O2/O3/O4 ratification list — confirm acceptable for supported deployment patterns and add it to the O-list / CHANGELOG.
- **Version traffic-collapse is a deliberate observable change.** Repeated identical-target calls now collapse to one `GET /` within the TTL (old code fetched every call). The returned value is identical but cluster traffic/latency observably changes — add to the O-list so the maintainer ratifies it.
- **`version_cache` single-mode key is per-URL/per-cluster, NOT per-identity.** `baseToolArgs` lets a call override creds without changing the URL, so two calls to the same URL with different credentials share one entry (and a negative result caches against the URL key). Almost certainly correct for "version is a property of the cluster," but confirm the invariant, tighten the docstring, and pin it with a same-URL-different-creds test. (Distinct from, and milder than, the header-auth MAJOR above.)
- **`version_cache` proportionality (carried from review #1).** 225 LOC (per-key locks, meta-lock, normalization, negative-floor, injectable clock) for a per-call `GET /` churn bug whose miss cost is one cheap request and whose `is_tool_compatible(None)=True` is fail-open. Confirm multi-cluster concurrency at scale is a real workload; if not, a plain TTL dict keyed by URL with a per-key (or no) lock would cut ~80 lines.
- **`generated/` granularity for the upcoming domains.** The 4 tools live in a 6-file package; defensible only if every future domain adopts the same `schema/params/handlers/register` quadrant. Decide the per-domain file shape now so `core.py`/`cat.py` don't inherit fragmentation that the cohesion doesn't warrant (≤~150-200 LOC domains likely want one file).

### VERDICT
**STOP-AND-FIX** — the `connection.py` `UnboundLocalError` regression (BLOCKER) masks real transport errors on a path with no test coverage; hoist the three locals + add the failing-session test before this branch advances. Then PROCEED-WITH-FIXES on the `params.py` validation drift/docstring and the `version_cache` header-auth key. The wired work is otherwise faithful and improved since review #1; the unwired ~600-LOC scaffold remains a sequencing concern, not a correctness one.
